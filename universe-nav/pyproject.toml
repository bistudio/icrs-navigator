[project]
name = "universe-nav"
version = "0.1.0"
description = "A Python navigation atlas for stellar and deep-space mapping."
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "pandas",
  "astropy",
  "astroquery",
  "plotly",
  "pyarrow"
]